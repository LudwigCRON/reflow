
module basic_wrapper (
    input wire      mcu_clk,
    input wire      mcu_warm_rstb,
    
    % for p in pins:
        % if p.type == "input":
    
        % endif
    % endfor

    % for p in pins:
        % if p.type == "output":

        % endif
    % endfor
);

endmodule